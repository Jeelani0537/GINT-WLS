- hosts: all
  gather_facts: no
  become: yes
  become_user: oracle

  roles:
  - role: ../roles/config-backup
    when: servers is search('Admin')

  - role: ../roles/baseline
    patch_file: 11G-patches-apr-2019
    when: servers is search('Admin')
